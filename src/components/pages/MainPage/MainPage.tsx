import React from 'react';

import Form from '../../Form/Form';

// /. imports

const MainPage: React.FC = () => {
    return (
        <div>
            <Form />
        </div>
    );
};

export default MainPage;
